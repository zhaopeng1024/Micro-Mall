spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.131.131:3306/micro_mall_pms
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
mybatis-plus:
  # mapper文件扫描
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto # 数据库主键自增
      logic-delete-field: flag  # 全局逻辑删除的实体字段名
      logic-delete-value: 0  #逻辑已删除值
      logic-not-delete-value: 1  # 逻辑未删除值
server:
  port: 8088
logging:
  level:
    com.pingan.life.micromall: debug