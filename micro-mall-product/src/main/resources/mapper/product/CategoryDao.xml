<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pingan.life.micromall.product.dao.CategoryDao">

    <resultMap type="com.pingan.life.micromall.product.entity.CategoryEntity" id="categoryMap">
        <result property="catId" column="cat_id"/>
        <result property="name" column="name"/>
        <result property="parentCid" column="parent_cid"/>
        <result property="catLevel" column="cat_level"/>
        <result property="showStatus" column="show_status"/>
        <result property="sort" column="sort"/>
        <result property="icon" column="icon"/>
        <result property="productUnit" column="product_unit"/>
        <result property="productCount" column="product_count"/>
        <collection property="subCategories" column="cat_id"
                    ofType="com.pingan.life.micromall.product.entity.CategoryEntity" select="selectSubCategories" />
    </resultMap>

    <select id="selectCategoriesWithTree" resultMap="categoryMap">
        select cat_id, name, parent_cid, cat_level, show_status, sort, icon, product_unit, product_count
          from pms_category
         where parent_cid = 0
           and show_status = 1
         order by sort
    </select>

    <select id="selectSubCategories" parameterType="int" resultMap="categoryMap">
        select cat_id, name, parent_cid, cat_level, show_status, sort, icon, product_unit, product_count
          from pms_category
         where parent_cid = #{catId}
           and show_status = 1
         order by sort
    </select>
</mapper>